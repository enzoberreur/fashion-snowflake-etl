# Dev dependencies — install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=7.4
pytest-cov>=4.1
pytest-mock>=3.12
ruff>=0.4
mypy>=1.8

# Optional, install separately if you need them:
# apache-airflow>=2.7
# apache-airflow-providers-snowflake>=5.0
# dbt-core>=1.7
# dbt-snowflake>=1.7
# great-expectations>=0.18
